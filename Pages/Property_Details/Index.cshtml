@page
@model PropertyManagement.Pages.Property_Details.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Property_Detail[0].Property_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Property_Detail[0].Area)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Property_Detail[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Property_Detail[0].Facilities)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Property_Detail) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Area)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Facilities)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
